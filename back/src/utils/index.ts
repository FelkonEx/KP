import {
  HUE_MAX,
  LIGHTNESS_MAX,
  SATURATION_MAX,
  RGB_MAX,
  COLOUR_TYPES,
} from "./constants";

import { GenerateRandomHSL, GenerateRandomRGB } from "./utils";

const constants = {
  HUE_MAX,
  SATURATION_MAX,
  LIGHTNESS_MAX,
  RGB_MAX,
  COLOUR_TYPES,
};

export { constants, GenerateRandomHSL, GenerateRandomRGB };
